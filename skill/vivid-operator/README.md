# Vivid Operator Skill

这个 Skill 面向 Codex、OpenClaw 和其他自动化 Agent。

它服务的不是单一 quickread 小项目，而是 `Vivid` 这个统一大项目。

它不承载核心业务逻辑，只暴露稳定的调用方式：

- 路径检查
- 环境检查
- quickread 执行
- 输出目录约定

推荐先调用：

- `paths`
- `doctor`
- `quickread`
